apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  namespace: app
spec:
  replicas: 2

  selector:
    matchLabels:
      app: frontend

  template:
    metadata:
      labels:
        app: frontend

    spec:
      #imagePullSecrets:
        #- name: ghcr-secret

      containers:
        - name: frontend
          image: ghcr.io/pasokatazip/frontend:778147dd35326eac08d0e6e9bc2ca7adb5810409

          ports:
            - containerPort: 3000

          env:
            - name: NEXT_PUBLIC_API_BASE_URL
              value: "https://api.akatukii.com"
