apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  namespace: app
spec:
  replicas: 2

  selector:
    matchLabels:
      app: frontend

  template:
    metadata:
      labels:
        app: frontend

    spec:
      #imagePullSecrets:
        #- name: ghcr-secret

      containers:
        - name: frontend
          image: ghcr.io/pasokatazip/frontend:b3e5f17e3b8bdee5c6e64e39f55469c3728b2278

          ports:
            - containerPort: 3000

          env:
            - name: NEXT_PUBLIC_API_BASE_URL
              value: "https://api.akatukii.com"
