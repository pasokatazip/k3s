apiVersion: batch/v1
kind: Job
metadata:
  name: backend-seed
  namespace: app
  annotations:
    argocd.argoproj.io/hook: PostSync
    argocd.argoproj.io/hook-delete-policy: HookSucceeded,HookFailed
spec:
  template:
    spec:
      restartPolicy: Never
      containers:
        - name: seed
          image: ghcr.io/pasokatazip/backend:5c270cfabd1e06daa1243bb97fd2bcca89205e13
          command:
            - sh
            - -c
            - |
              for f in seeds/common/*.sql; do
                echo "Applying $f"
                psql "$DATABASE_URL" -f "$f"
              done
          envFrom:
            - secretRef:
                name: backend-secret
