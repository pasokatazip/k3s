apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
  namespace: app
spec:
  replicas: 2

  selector:
    matchLabels:
      app: backend

  template:
    metadata:
      labels:
        app: backend

    spec:
      #imagePullSecrets:
        #- name: ghcr-secret

      containers:
        - name: backend
          image: ghcr.io/pasokatazip/backend:b0caebe4bc681bc59d250d67db361bf26ac6621f

          ports:
            - containerPort: 8080
          envFrom:
            - secretRef:
                name: backend-secret
